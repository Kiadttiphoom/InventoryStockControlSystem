@using InventoryStockControlSystem.ViewModels
@model CategoryViewModel

@{
    ViewData["Title"] = "Delete Category";
}

<div class="page-header">
    <div>
        <h1><i class="bi bi-trash me-2 text-danger"></i>Delete Category</h1>
        <div class="breadcrumb">Categories &rsaquo; Delete</div>
    </div>
    <a asp-action="Index" class="btn btn-outline-secondary"><i class="bi bi-arrow-left"></i> Back</a>
</div>

<div class="row">
    <div class="col-lg-5">
        <div class="card" style="border-color:#fca5a5;">
            <div class="card-header" style="background:#fff1f2;border-color:#fca5a5;">
                <h5 class="text-danger mb-0"><i class="bi bi-exclamation-triangle-fill me-2"></i>Confirm Deletion</h5>
            </div>
            <div class="card-body">
                <p style="color:#64748b;font-size:13.5px;">
                    Are you sure you want to delete category <strong>"@Model.Name"</strong>?
                    Products assigned to this category will be affected.
                </p>
                <div style="background:#f8fafc;border-radius:8px;padding:1rem;margin-bottom:1.25rem;border:1px solid #e2e8f0;">
                    <div class="row g-2" style="font-size:13.5px;">
                        <div class="col-4 text-muted">Name</div>
                        <div class="col-8 fw-600">@Model.Name</div>
                        <div class="col-4 text-muted">Description</div>
                        <div class="col-8">@(Model.Description ?? "-")</div>
                        <div class="col-4 text-muted">Status</div>
                        <div class="col-8">
                            @if (Model.IsActive) { <span class="badge badge-active">Active</span> }
                            else { <span class="badge badge-inactive">Inactive</span> }
                        </div>
                    </div>
                </div>
                <form asp-action="Delete" method="post" class="d-flex gap-2">
                    <input type="hidden" asp-for="Id" />
                    <button type="submit" class="btn btn-danger"><i class="bi bi-trash"></i> Yes, Delete</button>
                    <a asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
                </form>
            </div>
        </div>
    </div>
</div>
